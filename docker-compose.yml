version: '3'

services:
  web:
    build: .
    command: python ch2/manage.py runserver 0.0.0.0:8000
    volumes:
      - ./:/usr/src/app/
    ports:
      - 8000:8000
    env_file:
      - ./ch2/.env.dev